apiVersion: v1
kind: Service
metadata:
  name: nginx-np
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: nginx_app
  ports:
  - name: NodePort
    protocol: TCP
    port: 80
    targetPort: 80
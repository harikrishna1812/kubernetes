apiVersion: v1
kind: Service
metadata:
  name: nginx-np
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: nginx_app
  ports:
  - name: nodeport1
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 32768